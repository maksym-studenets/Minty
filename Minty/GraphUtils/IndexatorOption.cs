namespace Minty.GraphUtils
{
    /// <summary>
    /// This enumeration sets two working modes for graph's indexator
    /// </summary>
    public enum IndexatorOption
    {
        COST, HAS_PATH
    }
}
